# 本番は Render の Environment に入れる
APP_ENV=dev
DB_PATH=./data/app.db
CORS_ORIGINS=*

# メール送信（任意）
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@example.com

# パスワードリセットURL（本番はあなたのドメイン）
PUBLIC_BASE_URL=http://localhost:8000
